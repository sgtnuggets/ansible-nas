all: 
  vars:
    ansible_user: jake
  children:
    servers:
      vars:
        someVar: "testValue"
        ansible_connection: local
        ansible_host: localhost
      hosts:
        stinker:
          hostnamevar: stinker
        # streamweaver:
        #   hostnamevar: streamweaver
        # zima:
        #   hostnamevar: zima
        