all: 
  vars:
    ansible_user: jake
  children:
    servers:
      vars:
        someVar: "testValue"
      hosts:
        stinker:
          hostnamevar: stinker
          ansible_connection: local
          ansible_host: localhost
        # streamweaver:
        #   hostnamevar: streamweaver
        zima:
          hostnamevar: zima
        